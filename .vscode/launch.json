{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cppdbg",
            "request": "launch",
            "name": "LLDB Debugger",
            "program": "${workspaceFolder}/triangle.exec",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build",
            "MIMode": "lldb",
        },
    ]
}
